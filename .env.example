# VoidLine environment config
# Copy to .env and edit as needed

# Qdrant (only needed if using vector memory experiments)
# QDRANT_URL=http://localhost:6333

# Seed override (default: 42)
# VOIDLINE_SEED=42
